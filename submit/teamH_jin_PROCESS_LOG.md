# PROCESS_LOG — 작업 기록 (과정 70점의 핵심 근거)

> 표준 헤더(CLAUDE.md 등)를 로드했다면 에이전트가 알아서 채워 줍니다. 비면 직접 채우세요.
> 원칙: **실제로 시킨 프롬프트를 그대로 인용**할 것. 요약만 있으면 점수가 깎입니다.

## 작성자 정보 (개인별 로그 — 본인 것만)
- 팀명: teamH (멤버 3명: Jin, Han, Kim)
- 본인 이름(작성자): Jin (jin)
- 공통과제(우리 팀이 자동화한 반복 수작업): 지진해일 수치모델(ADCIRC 계열) 수행을 위한 `earthquake_model_setting`의 **10단계 전처리·실행 준비 과정**. 기존에는 작업자가 단계 폴더마다 직접 들어가 파일·코드·파라미터를 수동으로 수정하고 순차 실행하는 단순 반복 작업이었음.
- 내가 맡은 부분: **전체 통합 · UX/UI 디자인 · 에이전트**(`app/`). Next.js 통합 앱(좌측 파라미터 패널 UI·실행·시나리오 큐 + 우측 3D 뷰 결합), 에이전트 채팅(파라미터 셋팅값 설정, 모델 직접 실행 X), 백엔드(Han)·뷰(Kim) API 연동·통합. ※ 초기엔 백엔드 파이프라인을 맡았으나, 수치모델 도메인을 잘 아는 Han이 백엔드를 맡도록 역할 재조정(로그 #8).
- 자유과제(있으면): 동일 주제 내 확장(3D 시각화, 에이전트 채팅 기반 파라미터 설정).

> **이 로그는 본인 것만 작성**합니다. 각자 자기 PC·계정으로 작업해 개인 로그를 남기고, 제출 시 **영문 파일명** `<팀영문명>_<이름로마자>_PROCESS_LOG.md`(예: `teamA_kim_PROCESS_LOG.md`)로 저장하세요. **한글 파일명은 압축 시 깨지므로 금지** — 한글 팀명·이름은 위 '작성자 정보'에 적습니다. 운영자가 팀별로 모아 채점합니다(전원 참여 = 팀별 개인 로그 수).

## 효과 측정 (Before → After, 결과 ⑥ 채점용 — 형식 자유)

> **핵심**: 기존에는 10단계 폴더마다 들어가 **특정 변수(단층 케이스·SSP·기간·해상도 등)를 직접 바꿔** 실행해야 했지만, 지금은 **웹 플랫폼 GUI에서 변수를 설정**하거나 **에이전트에게 자연어로 변수 설정을 맡겨** 동일 작업을 한다. 수치모델 수행(계산) 시간 자체는 동일해 시간을 정량적으로 단축했다고 단정하긴 어렵지만, **이 방식 전환으로 폴더별 반복 수작업이 사라져 훨씬 효율적**이다. (정성 위주)

| 지표 | Before (수작업) | After (에이전트화) | 효과 |
|------|--------|-------|------|
| **케이스 전환 절차** | 단계 폴더 10개를 하나하나 들어가 파일·경로·케이스번호를 직접 열어 수정하고 순차 실행 | 파라미터 1개만 바꾸면 전 단계 설정에 자동 반영 | 폴더별 수동 반복 제거 |
| **파라미터·변수 설정** | 스크립트/텍스트 파일을 직접 편집(유효값을 알아야 하고 오기입 위험) | 좌측 폼·에이전트 채팅으로 설정(검증 포함) | 설정 간소화·실수↓ |
| **다중 시나리오 생성** | 케이스마다 작업자가 직접 값을 바꿔 반복 수행 | 여러 시나리오를 큐에 자동 생성 → 순차 자동수행 | 반복 작업 자동화 |
| **데이터 시각화 고도화** | 단계 폴더에 흩어진 수치/격자 파일을 수동 수집·해석 | 부산 디지털트윈 3D 침수(침수심별 색)·2D 최대수위(maxele)를 한 화면에서 | 결과 이해·비교 직관화 |

> 위 4개 지표는 팀 공통 `submit/BEFORE_AFTER.md`와 동일하다(개인 로그·공통 문서 일관성 유지).

## 사용 기법 (권장·가점, 필수 아님)
- [x] (a) 서브에이전트 / 역할 분담 — 10단계 스크립트를 3개 Explore 서브에이전트로 병렬 분석
- [x] (b) 외부 도구·데이터 연동 (파일/API/MCP/사내데이터) — 로컬 `earthquake_model_setting` 스크립트·데이터 직접 분석
- [x] (c) 재사용 산출물 (스킬 / 프롬프트셋 / CLAUDE.md / 서브에이전트 구성) — `submit/assets/STEP_ANALYSIS.md`, 루트 `PLAN.md`

---

## 작업 로그 (단계마다 1개씩 누적 / 시간순)

### [#1] 주제 정의 · 기존 10단계 수작업 분석 · MVP 범위 확정
- 작성자(팀원): Jin
- 목표: 해커톤 주제를 명확히 하고, 자동화 대상인 `earthquake_model_setting`의 10단계 구조를 파악해 구현 범위(MVP)와 기술 스택을 확정.
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "주제는 지진해일 수치모델을 수행하는데 … 10단계의 과정이 있고 기존에는 작업자들이 하나하나 들어가서 파일 또는 코드, 파라미터를 수정하고 수동으로 수행하는 과정을 단순 반복 … 웹 플랫폼 기반으로 해당 파라미터나 변수 설정을 쉽게 … 세팅한 값을 바탕으로 여러 시나리오를 자동적으로 수행 … 궁극적인 목적은 '기존 10단계의 단순 반복 및 번거로운 수작업을 자동화 하는 것'"
- 사용한 기법(있으면): (b 도구연동 — 로컬 `earthquake_model_setting` 스크립트/데이터 직접 분석)
- 결과: 10단계(`01_Deform_Plane`~`10_SLR_RUN`)가 모두 Python 스크립트 + 입력데이터(fort.13/fort.14, dat, xlsx)로 구성됨을 확인. 의사결정 확정 — ① MVP = 파라미터→10단계 자동 실행 파이프라인, ② 실제 수치모델 실행은 불가하여 기존 결과/Mock으로 흐름 시연, ③ 스택 = React 풀스택(Next.js) + Python 단계 오케스트레이션. 작성자 정보·Before/After 기록 완료.
- 막힘 → 해결: 파일명이 `HACKERTON.md`(대문자)라 처음 검색에 안 잡힘 → 루트 직접 조회로 확인.

### [#2] 10단계 스크립트 정밀 분석 (서브에이전트 3개 병렬)
- 작성자(팀원): Jin
- 목표: 각 단계가 받는 파라미터·입출력·단계 연결관계를 추출해 파라미터 스키마/파이프라인 설계 근거 확보.
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "각 단계 스크립트를 실제로 읽고 … ① 하는 일 ② 읽는 입력 파일/경로(하드코딩 포함) ③ 쓰는 출력 ④ 사용자가 바꿀 파라미터·변수(변수명·예시값 그대로) ⑤ `_2/_3/_4` 사본 차이 ⑥ 다음 단계 연결 … 코드는 수정하지 말고 읽기만 해."
- 사용한 기법(있으면): (a 서브에이전트 — Explore 3개로 01~04 / 05~07 / 08~10 병렬 분석), (b 로컬 코드·데이터 연동)
- 결과: 전체 흐름 확정(01 단층엑셀→02 변위→03 격자→03b 합산→04 조위보정→05 Manning→06 실행폴더 / 07 SSP→08 SLR보간→09 합산→10 실행폴더). 시나리오 변형축 = 지역(Ulsan/Jeju_north/Jeju_south/Jindo_Wando) × SSP(2.6/4.5/7.0/8.5) × 기간(near/far 등) × 지진해일 case(1~9). 모든 경로가 리눅스 절대경로 하드코딩이고 사본(`_2/_3/_4`,`_7.0`,`_last`)은 `Config` 변형임을 확인. 분석을 `submit/assets/STEP_ANALYSIS.md`로 정리.
- 막힘 → 해결: 없음(데이터는 서버에 있고 로컬엔 스크립트만 있어, 경로 파라미터화의 필요성을 오히려 명확히 확인).

### [#3] 개발 계획서 작성 · 3인 역할분담 · 아키텍처 확정
- 작성자(팀원): Jin
- 목표: MVP 범위/스택 결정에 맞춰 팀 공통 계획서와 역할분담을 문서화하고 git에 반영.
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "우선은 계획만 세워주고 3명의 역할분담을 해야하고 git push를 해줘야함"
- 사용한 기법(있으면): (c 재사용 산출물 — `PLAN.md`)
- 결과: 루트 `PLAN.md` 작성(아키텍처, 핵심결정, 3인 역할분담, 팀 인터페이스/API 계약, 마일스톤, 리스크). 역할 = Jin(백엔드 오케스트레이션/파이프라인·MVP핵심), Han(프론트엔드 좌측 파라미터 폼+에이전트 채팅), Kim(우측 부산권 3D 시각화). `submit/`을 통째로 무시하던 잘못된 `.gitignore`를 올바른 내용으로 교체.
- 막힘 → 해결: 기존 `.gitignore`가 UTF-16로 깨진 채 `submit/`을 무시 → 제출 폴더가 누락될 위험 → 표준 .gitignore로 교체해 해결.

### [#4] 해커톤 채점기준 파악 · 3인 공유저장소 협업 워크플로우 설계
- 작성자(팀원): Jin
- 목표: 운영진 notion 자료로 채점기준·제출규칙을 파악하고, 3인이 한 저장소를 공유하는 git 협업 방식을 정의.
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "../해커톤 notion에 채점이나 해커톤을 위한 정보가 있어 이거는 파악만 하면 되" / "해당 저장소는 3인 팀원이 서로 작성한걸 푸시하고 풀 하고 할거야" / "submit에서 process log만 개인별로 있고 나머지는 공통일건데 가능하면 올려서 공유되야함"
- 사용한 기법(있으면): (b 외부자료 연동 — notion export zip 파싱·분석)
- 결과: 채점 = 과정 70(①활용깊이25 ②재현·전파성25 ③전원참여10 ④문제정의10) : 결과 30(⑤완성도15 ⑥임팩트12 ⑦자유과제3). 과정은 PROCESS_LOG·재사용자산 근거로 AI 객관채점. 제출물 5종(산출물/Before·After/재사용자산/PROCESS_LOG/발표자료). git 컨벤션 = 브랜치 없이 전부 main, 충돌방지 규칙=본인 이름 파일만. → 협업설계 확정: PROCESS_LOG만 개인별, 그 외 submit·코드는 공통으로 main에 push해 공유. 코드는 역할별 폴더(backend=Jin/app=Han/viz=Kim)로 분리해 충돌 최소화. PLAN.md에 'Git 협업 워크플로우' 섹션 추가.
- 막힘 → 해결: notion export의 한글 파일명이 깨져 보였으나 본문은 UTF-8이라 내용 파악엔 문제없음. main 직접 push는 에이전트 안전정책으로 차단됨 → 사용자 승인 필요(공유 저장소 컨벤션이 main이므로 승인 권장).

### [#5] 팀원용 협업 가이드(TEAM_GUIDE.md) 작성 — 에이전트로 pull/push & 역할작업
- 작성자(팀원): Jin
- 목표: 팀원(Han·Kim)이 git을 몰라도 에이전트에게 말로 시켜 pull/push하고, 역할분담대로 본인 폴더에서 작업할 수 있게 실전 가이드 문서화.
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "협업 관련 문서에 팀원들이 에이전트로 push/pull 및 역할분담에 따른 작업 가능하게 명시 필요"
- 사용한 기법(있으면): (c 재사용 산출물 — TEAM_GUIDE.md)
- 결과: 루트 `TEAM_GUIDE.md` 작성 — ①클론 ②매작업 루틴(받고→만들고→올리기, 에이전트 자연어+명령어 병기) ③충돌0 규칙 ④역할별 시작 프롬프트(Jin=backend/Han=app/Kim=viz) ⑤마무리 체크. main push 차단 시 "메인에 직접 올려도 돼 승인할게"로 푸는 법, rejected 시 pull 후 재push까지 안내. PLAN.md에서 이 가이드를 링크.
- 막힘 → 해결: 없음.

### [#6] Git 협업 방식을 개인 브랜치→main 병합으로 전환 · 브랜치 3개 생성
- 작성자(팀원): Jin
- 목표: 충돌·안전정책 차단을 원천 차단하는 협업 방식으로 전환하고 팀원별 브랜치를 준비.
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "내거는 Jin 브랜치로 push해줘" / "그럼 jin kim han 브랜치를 만들고 최종적으로 main에 하면 되나?"
- 사용한 기법(있으면): (c 재사용 산출물 — 가이드 갱신)
- 결과: 워크플로우 확정 = 각자 본인 브랜치(`Jin`/`Han`/`Kim`)에서 작업·push → 준비되면 main에 병합(merge/PR). 본인 브랜치엔 혼자만 올려 충돌 0, main 직접 push 차단도 회피. `TEAM_GUIDE.md`·`PLAN.md`의 git 섹션을 브랜치 방식으로 갱신(본인 브랜치 전환, 일상 루틴, main 병합 절차, PR 안내). 원격에 `Jin`/`Han`/`Kim` 브랜치 생성.
- 막힘 → 해결: 없음.

### [#7] 팀원 브랜치 점검·main 병합 · Kim 로그 스타터 생성(전원 로그 submit 수집)
- 작성자(팀원): Jin
- 목표: 팀원 브랜치(Jin/Han/Kim) 상태를 점검해 main으로 병합하고, 각 개인 PROCESS_LOG가 `submit/`에 모이도록 정리.
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "kim han jin 브랜치를 확인하고 main으로 합치기 가능할까?" / "각 process log가 submit에 들어와야함"
- 사용한 기법(있으면): (b 도구연동 — git 브랜치 점검/병합)
- 결과: 점검 결과 Han만 1커밋 앞섬(본인 로그 신규 + timestamps 추가, 충돌 0) → fast-forward로 main 병합. Jin·Kim은 main과 동일해 병합 불필요. Kim 로그가 없어 `submit/teamH_kim_PROCESS_LOG.md` 스타터(작성자정보·역할 viz만 채운 빈 템플릿) 생성·병합. 이로써 main `submit/`에 teamH_jin/han/kim 세 개인 로그가 모두 수집됨. 모든 브랜치(main/Jin/Han/Kim)를 동일 지점으로 정렬.
- 막힘 → 해결: 공통 파일 `timestamps.txt` 충돌 방지를 위해, Jin 브랜치를 먼저 main으로 FF한 뒤 작업(전면 재생성 대신 append 방식 유지)해 Han 항목이 덮이지 않도록 처리.

### [#8] 역할 재분담(수치모델 도메인 반영) · 폴더별 통합문서 규칙 명시
- 작성자(팀원): Jin
- 목표: 팀원 전문성에 맞게 역할을 재조정하고, 마지막 통합이 쉽도록 각자 폴더에 작업기록(README.md)을 남기는 규칙을 문서화.
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "역할분담을 수정하자 han이 수치모델을 담당하고 있으니 파라미터나 수치모델 수행을 위한 과정을 알고 있어 그래서 해당 작업을 담당하고, kim은 지진해일 시뮬레이션 3d 등, 그리고 jin은 전체 통합 및 ux ui 디자인, 그리고 에이전트 부분을 담당" / "내가 마지막에 통합하기 좋게 각자 작업 내용이나 요소 등이 각 폴더에 md로 기록되도록 명시"
- 사용한 기법(있으면): (c 재사용 산출물 — PLAN/TEAM_GUIDE 갱신)
- 결과: 역할 재정의 — **Han=백엔드 수치모델 파이프라인·파라미터(`backend/`)**, **Kim=지진해일 3D 시뮬레이션 시각화(`viz/`)**, **Jin=전체 통합·UX/UI·에이전트(`app/`)**. PLAN.md 4·5·6·7절(역할/인터페이스/마일스톤/소유표)과 TEAM_GUIDE 역할 프롬프트를 새 기준으로 교체. **통합용 폴더 문서 규칙(7-2)** 신설: 각자 본인 폴더 `README.md`에 ①무엇을 만들었나 ②입력/출력(인터페이스) ③실행법 ④TODO를 항상 기록 → Jin이 이를 근거로 통합. 파라미터 스키마/결과 포맷 정의 주체를 Jin→Han으로 이전.
- 막힘 → 해결: 없음.

### [#9] 통합 웹 플랫폼 베이스(틀) 구축 — `app/` (Next.js)
- 작성자(팀원): Jin
- 목표: 파라미터 설정/3D는 Han·Kim이 채울 수 있도록, 통합 앱의 **틀(레이아웃·디자인시스템·연동 슬롯)**을 먼저 만든다.
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "@HACKERTON.md에 jiN 작업 내용을 적어놓았어 파악하고 베이스 작업 진행해" / "AI 티 나는 디자인 절대 금지 / 최소 12 Pretendard / 라이트 테마 고정" / "모델 설정 값이나 파라미터 설정 법 그리고 3d 시뮬레이션은 kim과 han이 만들고 나중에 push/pull … 틀만 먼저 만들어"
- 사용한 기법(있으면): (b 도구연동 — Next.js 스캐폴딩·빌드·Playwright 스크린샷 검증), (c 재사용 산출물 — app/README.md)
- 결과: `app/`에 Next.js 16 + React 19 + TS + Tailwind v4 앱 생성. 디자인시스템(라이트테마 고정·Pretendard·최소12px·마린블루 단일액센트, 그라데이션/글로우 배제). 레이아웃 = 상단바 + 좌측(실험 파라미터 placeholder → 시나리오 큐 → 에이전트 채팅) + 우측(Kim viz/ 3D 슬롯 placeholder + 침수심 범례·위치핀). 에이전트 채팅은 자연어→좌측 파라미터 자동설정(로컬 파서, 추후 Claude function-calling 교체). [실행] 시 10단계 Mock 진행률 표시. `npm run build` 통과·dev 200 렌더·스크린샷으로 시각 검증(submit/evidence/app_base_screenshot.png). 파라미터 실제값/검증은 Han, 3D는 Kim이 push/pull로 교체 예정 — 연동 지점·TODO를 app/README.md에 명시.
- 막힘 → 해결: SVG path가 % 좌표 미지원 → 바다 영역을 CSS clip-path로 교체. Segmented 제네릭이 string과 충돌 → 시그니처를 string 기반으로 단순화. 검증용 playwright는 끝나고 제거(팀원 설치 가볍게).

### [#10] 디자인 시스템 리서치·재디자인 (해양 운영 콘솔 · 또렷한 대비)
- 작성자(팀원): Jin
- 목표: "AI 티/흐리멍텅함" 배제 + 라이트테마·Pretendard·최소14px 유지하며 전문 콘솔 톤으로 재디자인.
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "폰트 전체적으로 키워줘 20% 정도 최소 14pt" / "배지나 버튼 그리고 각 텍스트 배치 컬러 등 너무 흐리멍텅하지 않게 필요한 스킬 및 플러그인 설치/활용하고 ui 요소 다양하게 찾아보고 검색해서 우리 컨셉에 맞는 것 활용" / "애매한 이모티콘 쓰지 말고 디자인 요소나 SVG 요소 객체 사용"
- 사용한 기법(있으면): (a 서브에이전트 2개 — 전문 콘솔 UI / 해양·침수심 팔레트 웹 리서치 병렬), (c 재사용 산출물 — DESIGN_TOKENS.md), frontend-design 스킬 활용
- 결과: 폰트 +20%·최소14px. 팔레트 = 네이비 잉크(#0A2540/#243B53) + 마린 블루 단일 액센트(#0E76C4) + 라이브 시안(#06B6D4) + 침수심 7단계 스케일, 보이는 보더(#8497A8). 흐림 해결(회색텍스트→네이비, 솔리드 배지/버튼, ring+dot). 이모지 0 — lucide 아이콘·SVG만. 시그니처 = 10단계 파이프라인 스테이지 레일. `globals.css`/`ui.tsx` 전면 교체, 토큰을 `submit/assets/DESIGN_TOKENS.md`로 자산화. Playwright 스크린샷으로 시각 검증.
- 막힘 → 해결: 침수심 범례가 스테이지 레일과 겹침 → 범례를 우상단으로, 핀을 바다영역으로 이동.

### [#11] 다중 시나리오 자동화(단일/배치) · 에이전트 함수호출 구성 · 마크다운 채팅 · UX 정리
- 작성자(팀원): Jin
- 목표: 단일 실행 + 여러 실험 자동 반복 수행을 모두 지원하고, 에이전트가 파라미터 설정·실험 설계·실행까지 하도록 구성. 화면 구분·전환 개선.
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "단일 실행 모드 및 시나리오 다중 실행? 여러 시나리오 자동 반복 수행? 기능이 두 가지 다 있어야 해" / "에이전트에게 파라미터 설정 요청도 할 수 있어야 하며, 여러 실험을 사용자가 지정하거나 알아서 하게 … 모든 요소 및 실험에 대한 기능 및 구성 준비" / "에이전트 채팅창은 렌더링 해서 표나 텍스트가 깔끔하게" / "에이전트랑 실험 탭을 위에 전환 … 가려지는 항목 없으면서" / "07_XXX 이런 파일 텍스트는 굳이 넣지 않는게" / "왼쪽 하단 N 버튼은 없애줘"
- 사용한 기법(있으면): (b 도구연동 — react-markdown/remark-gfm), (c 재사용 산출물 — 에이전트 툴 스키마 AGENT_TOOLS)
- 결과: ① 두 실행 모드(단일 즉시 / 큐 추가→전체 자동 실행). ② 에이전트 능력을 함수호출 4툴로 구성(set_parameters/queue_scenarios/run) — 단일 설정·스윕(SSP 전부·케이스 1~5·방향 등)·자동 설계·일괄 실행, 결과를 **마크다운 표**로 렌더. 실제 Claude function-calling으로 교체할 스키마 `AGENT_TOOLS` 명시. ③ 좌측 [파라미터]/[에이전트] 탭 전환(가림 없음), 우측 [3D 뷰]/[시나리오 비교] 탭(완료 결과 비교). ④ 파라미터 그룹 카드화(지진원/SLR/영역) + 파일코드 힌트 제거, 친화적 단계 라벨. ⑤ 지진원 파라미터(방향·Mw) 추가, 부산을 시각화 대상 기본값으로. ⑥ Next dev N버튼 제거(devIndicators:false). tsc 통과·스크린샷 검증.
- 막힘 → 해결: parseIntent 전면 교체 시 AgentChat/AppShell 인터페이스 동기화 필요 → 액션 디스패처(runAgent)로 정리.

### [#12] 에이전트 값목록 인식 강화 · 상단 큐 상태 가시성 · 텍스트 정리
- 작성자(팀원): Jin
- 목표: 사용자가 시나리오들을 말하면(전부/지정 목록) 큐에 자동 추가되도록 강화하고, 화면 군더더기 텍스트·가시성 개선.
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "사용자가 어떤 시나리오 들을 얘기하거나 다양성을 얘기하면 파라미터 탭의 아래 시나리오 큐에 해당 시나리오들을 자동으로 다 추가하는거지" / "파라미터 설정·다중 시나리오 자동 수행·부산권 3D 침수 시각화 이런 자잘한 텍스트들이 좀 별로야" / "위에 대기 실행 완료는 시나리오 큐 관련인 것 같은데 가시성이 별로네"
- 사용한 기법(있으면): (해당 없음 — 프론트 로직/UX)
- 결과: 에이전트 스윕이 '전부' 키워드뿐 아니라 **명시적 값 목록 ≥2개**도 인식(예: "SSP 2.6하고 8.5, 케이스 1 3" → 2×2=4개 곱집합 자동 큐). 상단 큐 상태(대기/실행/완료)를 [큐] 아이콘 + 굵은 컬러 숫자(실행=액센트, 완료=녹색)로 가시성 강화. TopBar 부제·섹션 설명 등 자잘한 텍스트 제거.
- 막힘 → 해결: "비교"가 전체 스윕 트리거에 들어가 지정 값 대신 전 범위(12개)로 펼쳐짐 → '비교'를 트리거에서 제외하고, "케이스 1 3" 다중 숫자 캡처로 수정.

### [#13] FHD·QHD·UHD 반응형 · 지역 부산권으로 정리 · 실행 버튼 3분할
- 작성자(팀원): Jin
- 목표: 모든 해상도에서 가시성 확보(UHD 텍스트 너무 작거나 FHD 요소 너무 크지 않게), 불필요 옵션·텍스트 정리, 실행 동작 명확화.
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "웹 플랫폼은 fhd qhd uhd에서 모두 가시성 좋게 … uhd에서 텍스트가 너무 작아서 보기 싫어지거나, fhd에서 요소들이 너무 커져서 다 쪼개지거나 겹치거나 텍스트가 너무 크거나 하지 않게" / "영역 대상격자도 우선은 부산[해운대, 마린시티] 정도만 … 울산이나 이런거는 필요 없고" / "육상부 텍스트도 필요없고 … 즉시 실행, 큐에 추가, 그리고 전체 큐 실행? 이런식으로 나눠야될듯"
- 사용한 기법(있으면): (해당 없음 — 반응형 CSS/UX)
- 결과: ① 루트 폰트 `clamp(16px, 0.4vw+8.3px, 22px)` + 고정 px를 rem으로 일괄 변환 → FHD/QHD/UHD에서 텍스트·간격·패널이 비례 스케일(최소 14px 보장). FHD/UHD 스크린샷 검증. ② 세부 파라미터는 전부 유지(다양한 실험·세부조건), 단 대상 지역은 부산 전체/해운대/마린시티만 남김(울산·제주 제거). ③ 실행 버튼을 [즉시 실행]·[큐에 추가]·[전체 큐 실행] 3개로 분리, 군더더기 설명문·'육상부' 힌트 제거.
- 막힘 → 해결: Tailwind 기본 간격은 이미 rem이라 루트 폰트만 반응형으로 두면 함께 스케일됨 → arbitrary px(text-[..px]/w-[420px])만 rem 변환해 해결.

### [#14] Han 백엔드 스키마 정렬 분석 · 고급 설정·지진원 방위·3D 애니메이션 대비
- 작성자(팀원): Jin
- 목표: Han 브랜치(읽기 참고)의 파라미터 스키마와 내 웹 선택항목을 정렬·보완하고, 향후 3D 애니메이션·백그라운드 실행에 대비한 프레임 준비.
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "HAN 브랜치를 참고해서 파라미터 설정 정보 파악하고 웹에 사용자 선택 가능한 것들 잘 되었는지 어떤게 추가가 필요한지 파악해" / "진앙지나 지진은 일본 쪽이나 남해 등에서 일어날텐데 우리가 3D 영역으로 볼 지역은 부산 지역에 한정 … 고려가 되어야 함" / "향후 3D 기반 시나리오 애니메이션 표출 대비 … 백그라운드 자동 실행 등 준비" / "남해·대한해협 이 텍스트도 왤케 얇지 pretendard가 아닌가"
- 사용한 기법(있으면): (b 도구연동 — origin/Han 읽기 참고, git show)
- 결과: ① Han `compat.py`가 내 types.ts와 1:1 호환(direction→sea_area, mw, caseNo→earthquake_cases, ssp, distance, period→backend distance, region 매핑, manningMode→manning_cut)임을 확인 — 핵심 정렬 양호. API(`POST/GET /api/run`, `/result` GeoJSON)·결과포맷(maxDepth/floodedArea/affectedBuildings)도 맞춰져 있음. ② Han `fort15_params.py`의 세부조건(시뮬레이션 기간·타임스텝·출력간격·관측소·마찰·SLR평균기간·MSL반경)이 웹에 없어 **고급 설정(ADCIRC·STEP) 접이식 섹션** 신설(placeholder, Han compat 입력필드 추가 합의 필요). ③ 지진원 방향을 '부산 기준 원거리 진앙 방면'으로 명확화(동=일본·동해/남=남해·대한해협/서=서해) — 원거리 진앙 vs 부산 국지 침수 관계 표시. ④ 3D 애니메이션 대비 **재생 타임라인(PlaybackBar)** 추가(완료 시나리오는 시간별 침수 애니메이션 재생 자리, Han timeseries 연동). ⑤ 한글이 font-mono로 떨어져 얇게 보이던 것 → Pretendard(sans)로 수정.
- 막힘 → 해결: 한글이 JetBrains Mono 미로드로 시스템폰트 폴백되어 얇아 보임 → 한글 들어가는 곳(Field 힌트·단위·단계명)은 sans로 전환, mono는 숫자·라틴 데이터에만 유지.

### [#15] 디지털트윈 통합·좌측 UX·시나리오 흐름 (app 통합)
- 작성자(팀원): Jin
- 목표: Kim의 3D 시각화(viz)·Han의 수치모델(backend)을 app으로 통합하고, 좌측 설정 → 우측 3D/2D/비교가 일관되게 동작하도록 데이터 흐름·UX를 정리.
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "플랫폼의 부산 영역 넓히고 … 물이 밀려들고 침수되는 범위의 침수 정도별 색을 표기하도록 해"
  > "시뮬레이션에서 보이는 영역은 바다쪽으로 더 확장 그리고 육지는 동백섬 이상 까지 확장"
  > "기존 5개 미리 만들어진 모의 시나리오는 없애고, 사용자가 좌측 메뉴에서 생성하면 그거 우측에서 볼 수 있게 … 바로바로?"
  > "케이스 없는데 … 정보가 표출되면 안되는데 그리고 3D 모델은 시나리오 없어도 기본 3D 객체는 나오도록 재생은 안되겠지만"
  > "거리 Near Far 이 부분은 필요 없대 분석 기간만 있으면" / "진앙과 규모 사이에 대상 지역도 … 주요 파라미터가 나오면"
- 사용한 기법: (a 서브에이전트 — 영역/랜드마크 bbox 검증 리서치 워크플로로 통합 기준 좌표 확정), (b Kim viz 정합 자산·Han backend 물리 파라미터를 app에 연동)
- 결과:
  - **영역/정합 기준 확정**: 리서치 워크플로로 해운대권 bbox·랜드마크를 교차검증해 통합 기준 좌표(동백섬 이상 육지+남측 외해)를 확정 → 위성/DEM/건물 재베이크(#17)가 동일 bbox로 정합되도록 기준 마련.
  - **좌측 파라미터 UX**: 거리(Near/Far) 제거(분석 기간만), 진앙·**대상지역**·규모·SSP·케이스·분석기간·최대침수심을 우측 카드에 한눈에.
  - **시나리오 흐름 리팩토링**: 미리 만든 프리셋 5종 제거 → `paramsToTsunami()`로 **사용자가 좌측에서 설정·실행한 시나리오를 그대로** 3D·2D에서 선택·표시(드롭다운=실행 목록). 실행 즉시 우측 반영, 완료 시 0부터 자동재생, 종료 후 다시 누르면 처음부터.
  - **미실행 처리**: 시나리오 미실행 시 상세/재생 숨김·기본 3D 객체만 표시(재생 비활성).
- 막힘 → 해결: 미실행인데 기본 파라미터 상세가 노출되던 문제 → `hasScenario` 게이팅으로 미실행 시 안내만 표시.

### [#16] 조작계·에이전트·서브에이전트 오케스트레이션·전문가 5인 리뷰·전기능 검증
- 작성자(팀원): Jin
- 목표: 조작/재생 UX를 사용자 지정대로 정리하고, 작업을 서브에이전트로 병렬화하며, 완성도를 전문가 점검·전기능 테스트로 보증.
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "조작 방법은 드래그 시점이동이고 컨트롤 누르고 드래그하면 회전, 휠은 줌인아웃으로"
  > "UI UX 디자인과 비쥬얼 조작성 사용자 편의성 등은 전문가 5인이 점검" / "모든 기능 구성요소 실제 테스트해서 작동 여부 확인 할 것"
  > "각 계획을 서브에이전트로 처리 불가능?" / "항상 스크린샷으로 확인해"
- 사용한 기법: (a 서브에이전트 — 전문가 5인(비주얼/UX/지오비주얼/접근성/도메인) 병렬 리뷰 워크플로(스크린샷+코드 판독→구조화 findings→종합); 2D 맵·셰이더 레시피 빌드는 별도 서브에이전트 워크플로로 병렬화), (b Playwright 전기능 동작 테스트·매 변경 스크린샷 검증)
- 결과:
  - **조작계**: 드래그=시점이동(팬), **Ctrl+드래그=회전**(pointerdown 캡처로 OrbitControls보다 먼저 버튼 매핑 결정), 휠=줌. 군더더기 안내 배지 제거.
  - **에이전트**: 자연어→파라미터 설정·다중 시나리오 스윕(예: "SSP 전부 비교"→큐 자동 추가), 마크다운 표 렌더.
  - **전문가 5인 점검**: 38개 findings→우선순위 12개 종합 반영(외해/연안 수치 의미 분리, "얕음·안전"→"주의", 세그먼트 접근성 focus ring·role 등).
  - **전기능 동작 테스트**: 좌측 탭/파라미터/고급설정/즉시실행·큐·전체실행/3D·2D·비교 전환/시나리오 선택/재생/에이전트 — **11/12 PASS·콘솔에러 0**.
- 막힘 → 해결: drei OrbitControls의 ctrl 회전 미동작 → 캡처단계 pointerdown 리스너로 수정자 선반영. 검증 working dir 혼선 → app 디렉토리 명시로 해결.

### [#17] 부산 디지털트윈 3D/2D 시각화 직접 구현 — 위성·DEM·건물 정합·GPU 물·2D maxele (app/src/viz)
- 작성자(팀원): Jin
- 목표: app 통합 플랫폼 우측의 3D/2D 시각화를 실제 공개자료로 정합 재구축하고, GPU 물·침수심별 색·곡선 쓰나미 파면·2D 최대수위 지도를 직접 구현.
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "위성 지형 수심 건물 모형 위치 정확하게 매치시키고 시뮬레이션 데이터 다시 현실적으로 생성 … 침수 정도별 색을 표기"
  > "물 시뮬레이션 퀄리티는 비주얼적으로 업그레이드 … gpu 기반으로 자연스럽고 현실적이도록"
  > "지형 높이에 따른 지형 수심 변화가 3d에 나타나야해" / "일직선으로 퍼지는데 … 부채꼴로 휘어서" / "침수 범위 색도 기본 물높이에서는 없도록" / "2D … fort14 격자 영역대로 … max 수위만 한장"
- 사용한 기법: (a 서브에이전트 — bbox/랜드마크 리서치·GPU 물 셰이더 레시피·2D maxele 빌드 워크플로 병렬), (b 외부 데이터 — ESRI 위성·AWS Terrarium DEM·OSM 건물), (c 재사용 산출물 — 공통 `scripts/bbox.mjs` + 재현 베이크 스크립트)
- 결과: `app/src/viz/`에 구현. ① 위성·DEM·건물을 단일 bbox·동일 등각투영으로 재베이크(건물–지도 정합), Terrarium 연안 0m를 바다마스크+합성수심으로 복원. ② 지면을 DEM으로 정점 변위(지형·수심 기복), 건물 지형 위 안착. ③ GPU `ShaderMaterial`(Gerstner 스웰 + 먼 발생원 방사거리 기반 곡선 파면), 침수는 DEM에서 η>지반고 판정·침수심별 위험색(외해=수심 블루), 침수색은 쓰나미로 정상 해수면 위 육지가 잠길 때만. ④ 2D 모델맵: 실측 지역 DEM 마스크 기반 fort.14 도메인 maxele 필드 + 진앙·대상지 마커. per-fragment GPU 전환·매 변경 스크린샷 검증·콘솔에러 0.
- 막힘 → 해결: Terrarium 연안 0m로 바다가 육지처럼 → 바다마스크+합성수심. 수직 과장이 건물 잠김 → 육상 실수면·외해만 과장 분리. 기본 물높이 침수색 → 쓰나미 기준으로 변경.

### [#18] 시연 UX 다듬기 · 시나리오 영속저장 · 에이전트 유연화 · 크로스플랫폼 자립 패키징
- 작성자(팀원): Jin
- 목표: 실사용·시연 피드백을 반영해 결과 표출 타이밍/조작을 정리하고, 생성 시나리오를 영속 저장하며, 에이전트를 더 유연하게 만들고, `src`만으로 어느 PC(mac/Win)에서나 구동되도록 패키징.
- 에이전트에게 시킨 것(실제 프롬프트 핵심 인용):
  > "시나리오 비교 탭은 없어도 될 듯 해"
  > "시나리오 실행 안했는데 2D 맵을 들어가면 떠있어 그건 안돼 … 모든것은 시나리오 생성 된 것만 - 3D 2D 나오도록"
  > "시나리오 큐 가 늘어나면 좌패널 전체 스크롤 바가 늘어나는데 시나리오 큐 내에 스크롤바를 하나 더"
  > "생성된 시나리오 리스트는 저장해두게 … 새로고침하거나 뭐 해도 기존에 만들었던 시나리오는 남아 있도록" / "github에는 데이터베이스 올릴 필요 없고 … 사용자 폴더에 생성되게"
  > "에이전트가 조금 더 유연했으면 … 사용자가 대충 요청해도 찰떡같이 알아들어서 이걸 원해? 이걸 할까? 하는 등으로"
  > "src 내에는 작동하는데 필요한 지도나 모형 등등 모든 것들은 필요해 또한 requirements.txt를 통해 … 안내" / "웹은 맥과 윈도우 모두 지원" / "시나리오 비움을 해도 오른쪽에서는 조회가 되어야"
- 사용한 기법: (b 도구연동 — Next API Route 파일 DB / Playwright 스모크 검증), (c 재사용 산출물 — `requirements.txt`·README 설치가이드)
- 결과:
  - **시연 타이밍**: 2D 모델맵을 **시나리오 실행(완료) 후에만** 표출(미실행 시 안내 placeholder). 3D는 기본 지형 트윈은 유지하고 침수 결과만 완료 후 표출.
  - **비교 탭 제거**: 우측 탭을 `3D 뷰 / 2D 모델맵`만 남김(`CompareTab` 등 관련 코드 제거).
  - **시나리오 큐 자체 스크롤**: 큐가 길어져도 좌패널이 무한정 늘어나지 않도록 큐 내부 `max-h + overflow` 스크롤 추가.
  - **영속 저장(개인 폴더 DB)**: `src/app/api/scenarios`(Next Route) ↔ `database/scenarios.json` 읽기/쓰기 + `lib/scenarioStore.ts`(서버 DB 1순위·localStorage 폴백). 새로고침·재실행해도 유지, **src 복사 시 데이터 동행**, git 미추적(개인 실험). `완료 비우기`는 삭제 대신 archived 처리해 **우측 뷰어에서는 계속 조회 가능**.
  - **에이전트 유연화**: 진앙 발생해역(일본·남해 등)·정성 규모(강진/약한)·분석기간·정성 SSP까지 인식 폭 확대, 못 알아들으면 실패 대신 **“이걸 원하셨나요?” 추측 되묻기** + 입력창 위 **빠른 예시 칩**.
  - **크로스플랫폼 자립**: 지도/DEM/건물(`public/twin/*`) 포함 확인, `requirements.txt`(설치 안내)·README 정비. macOS·Windows 공통(`path.join`·Node/Next). `tsc --noEmit` 통과.
- 막힘 → 해결: 칩 onClick이 MouseEvent를 `send(textArg)`로 넘겨 타입오류 → `onClick={() => send()}`로 수정. 영속 저장이 진행률 틱마다 쓰지 않도록 700ms 디바운스(완료 시 1회 기록).

### [#19] ...
(필요한 만큼 계속 추가)

---

## 마무리 요약 (1~2줄)
- 가장 효과적이었던 에이전트 활용법: 작업을 **서브에이전트로 병렬화**(10단계 스크립트 분석·전문가 5인 UI/UX 리뷰·영역 bbox 리서치·GPU 물 셰이더/2D maxele 빌드)하고 **매 변경을 Playwright 스크린샷으로 즉시 검증**(콘솔에러 0)한 것. 자연어→파라미터·시나리오 큐 설계를 에이전트에 맡겨(되묻기·칩) 비전문가 접근성을 높였다.
- 다른 팀이 그대로 따라 하려면 필요한 것: **역할별 폴더 분리(app/backend/viz)+폴더 README 인터페이스 규칙**으로 통합 지점을 먼저 고정하고, 공개자료(ESRI 위성·AWS Terrarium DEM·OSM 건물)를 **단일 bbox로 재현 베이크하는 스크립트화**, 그리고 **`src` 자립 패키징**(어느 PC·mac/Win에서든 `npm install`→`npm run dev`, 시나리오는 `database/`에 영속).
